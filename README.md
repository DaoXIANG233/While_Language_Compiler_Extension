# While_Language_Compiler_Extension

A simpile while language extension, compiled with python 3.

The compiler targets the LLVM intermediate instructions which can then be easily compiled to executables.